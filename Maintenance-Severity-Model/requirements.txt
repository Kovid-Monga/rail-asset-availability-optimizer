# Core dependencies -----------------------------------------------------------
# Lower bounds rather than exact pins: this is a library-style component that
# has to sit inside a larger prioritisation service without forcing a
# downgrade on it. Pin exactly (==) in your deployment image.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7          # confusion-matrix PNGs only

# Optional ---------------------------------------------------------------------
# nltk provides WordNet lemmatisation. If it is absent, or its corpora cannot be
# downloaded (air-gapped server), preprocess.py falls back to a rule-based
# lemmatiser automatically and the pipeline still runs.
nltk>=3.8

# xgboost is compared against Logistic Regression and Random Forest when
# installed. train.py skips it silently if the import fails, so you can drop
# this line on machines where the wheel is unavailable.
xgboost>=2.0

# After installing, optionally fetch the WordNet corpora once:
#   python -m src.preprocess --setup
